# source/graf/CMakeLists.txt
# Root CMakeLists for the graf external family.
# Lives at source/graf/ inside the Max SDK tree.
# Add each external as a subdirectory below.

add_subdirectory(graf)
add_subdirectory(graf.traverse)
add_subdirectory(graf.affiche)